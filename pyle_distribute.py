from os import listdir
import time

def distribute(directory, parameter, options):
    parameter_list[parameter](directory, options)
    return

def time_distribute(directory, options):
    os.listdir(directory, options)
